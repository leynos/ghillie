apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghillie.fullname" . }}
  labels:
    {{- include "ghillie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghillie.selectorLabels" . | nindent 4 }}
